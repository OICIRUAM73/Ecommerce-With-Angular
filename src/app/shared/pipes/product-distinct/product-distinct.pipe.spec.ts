import { ProductDistinctPipe } from './product-distinct.pipe';

describe('ProductDistinctPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductDistinctPipe();
    expect(pipe).toBeTruthy();
  });
});
